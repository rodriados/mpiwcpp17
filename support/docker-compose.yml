services:
  test:
    image: mpinode
    networks:
      - mpinet
    volumes:
      - .:${PWD}
    deploy:
      mode: replicated
      replicas: ${REPLICAS:-4}

networks:
  mpinet:
    name: mpinet
    driver: bridge
